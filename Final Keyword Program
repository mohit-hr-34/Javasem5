Class A
{ final int speedlimit=60;
   void display()
   { speedlimit=100;
     System.out.println(speedlimit);
   }
   public static void main(String args[])
    { A a1=new A();
      a1.display();
     }
}
OUTPUT:- It will raise an error becoause of speedlimit different values
Note:-1) If we use final keyword with methods, the methods can not be overridden its derived class.
      2) A class with final keyword can not be inherited further.
